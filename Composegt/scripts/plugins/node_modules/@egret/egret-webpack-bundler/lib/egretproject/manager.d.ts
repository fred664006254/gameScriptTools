import { Target_Type } from './data';
export declare namespace manager {
    function copyToLibs(): void;
    function generateManifest(gameFileList: string[], options: {
        debug: boolean;
        platform: "web" | "native";
    }): {
        initial: string[];
        game: string[];
    };
    function generateManifest(gameFileList: string[], options: {
        debug: boolean;
        platform: "web" | "native";
    }, manifestPath?: string): void;
    function copyLibsForPublish(target: Target_Type, mode?: "debug" | "release"): string[];
    function copyManifestForNative(toPath: string): void;
    function modifyIndex(manifestPath: string, indexPath: string): void;
}
